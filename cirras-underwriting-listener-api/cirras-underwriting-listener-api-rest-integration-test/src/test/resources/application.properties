webade-oauth2.token.url=${WEBADE_GET_TOKEN_URL}
webade-oauth2.authorize.url=${WEBADE_AUTHORIZE_URL}
webade-oauth2.check.token.url=${WEBADE_CHECK_TOKEN_URL}
test.client.id=${CIRRAS_UNDERWRITING_SYNC_USER}
test.client.secret=${CIRRAS_UNDERWRITING_SYNC_SECRET}

// TODO replace with the URL of the service under test
context.wfhr.diary-submission-listener-rest=${WFHR_DIARY_SUBMISSION_LISTENER}

activemq.server.url=${ACTIVEMQ_SERVER_URL}
activemq.userName=${ACTIVEMQ_USERNAME}
activemq.password=${ACTIVEMQ_PASSWORD}
