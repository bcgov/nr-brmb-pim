webade.datasource.url=${webade.datasource.url}
webade.datasource.username=${webade.datasource.username}
webade.datasource.password=${webade.datasource.password}

failover.datasource.url=${POSTGRES_URL}
failover.datasource.username=${POSTGRES_USERNAME}
failover.datasource.password=${POSTGRES_PASSWORD}

legacy-wps.datasource.url=${legacy-wps.datasource.url}
legacy-wps.datasource.username=${legacy-wps.datasource.username}
legacy-wps.datasource.password=${legacy-wps.datasource.password}

#activemq.server.url=vm://localhost
